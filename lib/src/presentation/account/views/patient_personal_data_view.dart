import 'package:flutter/material.dart';

class PatientPersonalDataView extends StatelessWidget {
  const PatientPersonalDataView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
